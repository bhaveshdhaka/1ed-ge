---
description: Typecheck and build the 1ed.ge site.
---

Run `npm run typecheck` and `npm run build`. Fix any errors before finishing.
If the build produces content-collection "empty" warnings that is expected —
empty directories are valid. Report the final result.
