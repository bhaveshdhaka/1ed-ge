---
description: Start the Astro dev server on :4321.
---

Run `npm run dev`. Confirm it starts on http://localhost:4321 and tell the user.
Do not run a production build. (Note: the dev server picks up content changes
live; the admin API works in dev too since it uses fs directly.)
