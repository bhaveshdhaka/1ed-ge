---
/** Capsule.astro — accent-tinted pill chip. */
interface Props {
  class?: string
  style?: string
}
const { class: cls = '', style } = Astro.props
---
<span class={`capsule ${cls}`} style={style}><slot /></span>
