---
/** Well.astro — inset recessed surface. */
/**
 * @example
 * <Well class="p-3">Recessed inset panel content.</Well>
 */
interface Props {
  class?: string
}
const { class: cls = '' } = Astro.props
---
<div class={`well ${cls}`}><slot /></div>
