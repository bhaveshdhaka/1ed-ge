---
export const prerender = false
import { fmtDay, isIsoDay } from '../../lib/dates'

const slug = Astro.params.slug ?? ''

if (!isIsoDay(slug)) {
  return new Response('not found', { status: 404 })
}

return Astro.redirect(`/day/${fmtDay(slug)}`, 301)
---
