# v0.3-alpha — Design skill, /design catalog, functional /status

**Status:** approved  
**Date:** 2026-08-11  
**Version surface:** design track only — published on `/dev` as `v0.3-alpha`.  
No `package.json` bump.

## Goal

1. Make `/status` fully functional in Docker via a **host snapshot sidecar**.
2. Truth-up the design skill to match live CSS/components.
3. Publish the full design system on `/design` (live primitives + composites).
4. Publish the increment on `/dev` as **v0.3-alpha**.

## /status — host snapshot sidecar

SSR runs inside Docker. Host `/tmp`, git, docker, and uptime are invisible.
Fix: host-only collector writes `data/status.json` (already bind-mounted).

| Piece | Detail |
|---|---|
| Script | `scripts/status-snapshot.mjs` (host only) |
| Output | `data/status.json` (atomic write) |
| Cron | every 60s + once on prod deploy |
| Page | reads snapshot only — no `execSync`, no host `/tmp` |
| JS | none (SSR only) |

Market `lastDay` = newest `src/content/market-news/YYYY-MM-DD.md` (content is truth).  
Stale badge if snapshot age > 5 minutes.

## Design skill + /design

- Skill matches `app.css` (accent `#0af`, radius 14/10, full type scale).
- No ghost `ui/react/*` claims — document real admin kit.
- `/design` = live catalog: tokens, all primitives, DayFacts + ThoughtCard fixtures, rules, backlog, changelog → v0.3-alpha.

## /dev

- `VERSION = 'v0.3-alpha'`
- Changelog + costs row for this increment.

## Non-goals

- No package.json version change  
- No docker.sock / full `/tmp` bind-mount into app  
- No client poll JS on `/status`  
